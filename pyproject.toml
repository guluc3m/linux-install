[project]
name = "linux-install"
version = "0.1.0"
description = "Guía del GUL-UC3M para la instalación de Linux"
requires-python = ">=3.11"
dependencies = [
    "zensical>=0.0.57",
]

[tool.uv]
package = false
